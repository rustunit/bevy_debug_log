[package]
name = "bevy_debug_log"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = { version = "0.14", default-features = false, features = [
    "default_font",
    "bevy_ui",
] }

[dev-dependencies]
pretty_assertions = "1"
bevy = "*"
